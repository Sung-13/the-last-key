# The Last Key — project memory

iOS app called **The Last Key** — helps the user memorise English sentences/expressions (with Korean meanings) as a 2–3 minute morning ritual. Frontend-only, SwiftUI + SwiftData, iOS 17+, no backend, no cloud, no analytics.

## Current state (as of 2026-07-16)

**Fully smoke-tested and visually redesigned.** The app now uses the "Dawn Warm" design language: warm cream background (`DawnBackground`), amber→coral sunrise gradient on primary buttons and the Today hero card, rounded typography, warm charcoal dark mode, and a sunrise-key app icon (light/dark/tinted variants). All design tokens live in `TheLastKey/TheLastKey/Theme.swift` (colors are asset-catalog sets with light+dark variants: DawnBackground, DawnSurface, DawnAmber, DawnAmberDeep, DawnCoral, AccentColor). Icon PNGs are generated by `tools/make_icon.swift` (run: `swift tools/make_icon.swift <path-to-AppIcon.appiconset>`).

Verification status: BUILD SUCCEEDED; 14 unit tests pass (8 ClozeFormatter + 6 PracticeQueue); UI smoke suite `TheLastKeyUITests/SmokeScreenshotTests` passes in light and dark mode and covers: seed → Today → session → reveal → grade (asserts the missed card re-queues within the session), Library add/search/delete, empty-library CTA → add sheet with autofocus, Settings. Dynamic Type (accessibility-medium) and home-screen icon visually checked.

Xcode project at `TheLastKey/TheLastKey.xcodeproj`, **Xcode 16 format with `PBXFileSystemSynchronizedRootGroup`** — any `.swift` file dropped into `TheLastKey/TheLastKey/` (app), `TheLastKey/TheLastKeyTests/`, or `TheLastKey/TheLastKeyUITests/` is automatically a member of that target. Build settings still require explicit `project.pbxproj` edits. Deployment target iOS 17.0.

Build: `xcodebuild -project TheLastKey/TheLastKey.xcodeproj -scheme TheLastKey -destination 'platform=iOS Simulator,name=iPhone 17' build`. Newest simulator on this machine is iPhone 17 (iOS 26.5); `iPhone 16` is NOT installed. Bundle ID is `com.sung13.TheLastKey`.

**Running the UI smoke tests with correct appearance:** `xcodebuild test -only-testing:TheLastKeyUITests/SmokeScreenshotTests -parallel-testing-enabled NO` — parallel testing must be OFF, otherwise tests run on a cloned simulator that ignores `simctl ui <udid> appearance dark` and discards data mutations. Boot the device (`simctl boot`) *before* `simctl ui … appearance …`; the command fails with "Unable to lookup in current state: Shutdown" on a shut-down device. Screenshots are XCTAttachments — export with `xcrun xcresulttool export attachments --path <xcresult> --output-path <dir>` (writing files from the test runner to host paths is sandbox-blocked). Note the suite mutates data (deletes/re-adds entries); `simctl uninstall` + reinstall restores the first-launch seed.

**TTS-on-simulator gotcha:** if `▶ Listen` plays nothing on the iOS Simulator, check (a) Mac system volume isn't muted, (b) Simulator menu → **Device → Audio Output** is set to a real output (not "None"). TTS output itself still needs human ears on hardware — XCUITest can't assert audio.

Hosted on GitHub at **[Sung-13/the-last-key](https://github.com/Sung-13/the-last-key)** (default branch `main`). Git identity: `Sung Kim <207924699+Sung-13@users.noreply.github.com>`.

## Confirmed product decisions (do not re-litigate)

| Area | Decision |
|---|---|
| App name | The Last Key |
| Memorisation technique | First-letter cueing + audio (iOS TTS via `AVSpeechSynthesizer`) |
| Unit of study | Full sentences (or shorter expressions) — not separate "phrase + example" |
| Daily session size | 5 cards (configurable 3–15 in Settings) |
| Session ordering | `Review again` first → unseen → least-recently-seen |
| In-session re-queue (2026-07-16) | "Review again" sends the card to the back of *today's* stack until remembered once; it stays flagged for a future session regardless (`PracticeQueue`) |
| Visual direction (2026-07-16) | "Dawn Warm": cream/amber/coral sunrise palette, rounded type, warm charcoal dark mode — chosen by user over Ink & Paper / Midnight Gold / Fresh & Playful |
| Input | Manual entry in-app only |
| Storage | Local on-device only — SwiftData |
| Platform | iPhone, iOS 17+, SwiftUI, SwiftData, AVFoundation. Zero external dependencies. |

## Explicit out-of-scope (non-goals)

- Backend / cloud sync / iCloud
- Accounts, analytics, third-party services
- Bulk or CSV import
- Streaks, charts, gamification (retention comes from visual delight instead)
- AI-generated examples or translations
- Push notifications

## Key references

- **Design/redesign plan:** `/Users/sungkyungkim/.claude/plans/plan-on-how-to-vectorized-treasure.md` (review + Dawn Warm redesign, 2026-07-16); original design: `/Users/sungkyungkim/.claude/plans/context-i-would-like-foamy-crab.md`.
- **Source tree:** App sources at `TheLastKey/TheLastKey/` (views in `Views/`, logic in `Services/`, design tokens in `Theme.swift`), unit tests at `TheLastKey/TheLastKeyTests/`, UI smoke tests at `TheLastKey/TheLastKeyUITests/SmokeScreenshotTests.swift`. `PreviewSamples.swift` is `#if DEBUG`-gated. `Services/InitialSeed.swift` seeds 7 starter entries on first launch, guarded by `@AppStorage("didSeedInitialEntries")` AND an empty-library check.
- `ContentView` owns tab selection + the add-entry sheet flag so Today's empty-state CTA can jump to Library and open the sheet. `SpeechService.shared` is the single app-wide synthesizer (per-view instances caused audio to be cut/leak).
- **xcodeproj editing:** Use the `xcodeproj` Ruby gem (installed at `~/.gem/ruby/2.6.0/`) for programmatic `.pbxproj` changes; synchronized groups mean file additions don't need pbxproj edits.

## Next steps remaining

1. Run on a physical iPhone to hear real TTS quality and feel the haptics (simulator can't verify either).
2. Uncommitted work: the 2026-07-16 redesign (all views, Theme.swift, color sets, icon, PracticeQueue + tests, smoke UI tests, tools/make_icon.swift) is in the working tree — commit when the user is happy with the look.
